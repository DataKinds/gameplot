[project]
name = "gameplot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.2",
    "psycopg[binary,pool]>=3.2.11",
]

[tool.uv.workspace]
members = [
    "temporal",
    "hello-temporal",
]

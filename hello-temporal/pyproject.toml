[project]
name = "hello-temporal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
    "temporalio>=1.24.0",
]
